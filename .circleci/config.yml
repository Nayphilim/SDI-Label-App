 version: 2.1

 orbs:
  win: circleci/windows@2.2.0
  
  machine:
  environment:
    QMAKE: /opt/qt54/bin/qmake

 jobs:
   build:
     executor: win/default     
    
     steps:
       - checkout
       - run: dotnet build
